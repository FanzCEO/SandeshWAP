# FANZ Devspaces — Phase 2

Adds:
- Templates Catalog (Node / FastAPI / Next.js)
- Projects API (create/list under /projects/<id>)
- AI helpers (dryrun/logs/dockerfile stubs)
- Same split-pane Editor + Terminal

Run:
```
npm install && npm run dev
```
Then in the terminal:
```
cd projects/<id>
npm install && npm start   # Node/Next.js
# or
pip install -r requirements.txt && uvicorn main:app --host 0.0.0.0 --port 8000  # FastAPI
```